apiVersion: 1

providers:
  - name: BotProve
    orgId: 1
    folder: BotProve
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
